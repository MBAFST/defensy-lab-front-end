let detectionType: number = 0;
let description: string = "";
let members: string = "";

export const createResume = () => {
    return {
        detectionType,
        description,
        members
    }
}
