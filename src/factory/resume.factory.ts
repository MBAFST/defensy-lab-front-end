let detectionType: string = "";
let description: string = "";
let members: string = "";

export const createResume = () => {
    return {
        detectionType,
        description,
        members
    }
}
